//Default, only one use per module, only for saving data types and values. There is no need to reference it in 
//  your import with the same name. To invoke its internal values it is used as an object.
export default{
    PORT:3000,
    number:34,
    string:"ALi"
    };

 